spring:
  datasource:
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: validate

server:
  port: 8088

kafka:
  bootstrap-servers: localhost:9092
